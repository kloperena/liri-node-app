console.log('this is loaded');

exports.spotify = {
  id: process.env.SPOTIFY_ID,
  secret: process.env.SPOTIFY_SECRET
};

// v = new Spotify(keys.spotify);

// var bands = new bands(keys.bands)

